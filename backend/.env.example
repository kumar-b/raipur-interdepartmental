#!/usr/bin/env bash
# Raipur Interdepartmental Portal — Environment Config
# Copy this file to .env and fill in real values.
#   cp .env.example .env

# ── Required ───────────────────────────────────────────

# Secret for signing JWT tokens. Must be at least 32 characters.
# Generate one with:  node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# ── Optional ───────────────────────────────────────────

# Server port (default: 3000)
PORT=3000

# Node environment: development | production
NODE_ENV=production

# Comma-separated list of allowed CORS origins.
# Leave empty to allow same-origin only (recommended for single-domain deployment).
# Example: https://portal.raipur.gov.in,https://admin.raipur.gov.in
ALLOWED_ORIGINS=

# ── Seed-only (development) ───────────────────────────

# Override default seed passwords (ignored in production — seed.js won't run).
# SEED_ADMIN_PASSWORD=YourStrongAdminPassword
# SEED_DEPT_PASSWORD=SharedDeptPassword
