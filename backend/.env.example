# ── Required ──────────────────────────────────────────────────────────────────
# Secret key for signing JWT tokens. Must be at least 32 characters.
JWT_SECRET=replace_with_a_long_random_secret_key_min_32_chars

# Port the Express server listens on (default: 3000)
PORT=3000

# ── File Storage — AWS S3 (optional) ──────────────────────────────────────────
# If all three AWS variables below are set, uploaded files (attachments and
# replies) are stored in S3. If any are missing, files fall back to local disk
# storage in the /uploads folder.

# AWS IAM credentials with s3:PutObject permission on the bucket
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key

# S3 bucket name (must already exist)
AWS_S3_BUCKET=your_bucket_name

# AWS region where the bucket is located (default: us-east-1)
AWS_REGION=ap-south-1
